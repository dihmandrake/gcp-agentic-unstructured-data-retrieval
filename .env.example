# ------------------------------------------------------------------------------
#
#                            --- INSTRUCTIONS ---
#
#  1. Copy this file to a new file named .env in the same directory.
#  2. Replace the placeholder values (e.g., "your-gcp-project-id") with your
#     actual configuration details.
#
# ------------------------------------------------------------------------------

# --- Google Cloud Platform Configuration ---

# The unique identifier for your Google Cloud project.
# Find this in the Google Cloud Console dashboard.
PROJECT_ID="your-gcp-project-id"

# The geographic location for your Vertex AI Search resources.
# Use "global" for multi-region, or a specific region like "us" or "eu".
LOCATION="global"

# The ID of your Vertex AI Search Data Store.
# This is created by the `make create-datastore` command.
DATA_STORE_ID="your-datastore-id"

# The ID of your Enterprise Search App/Engine.
# This is created by the `make create-engine` command.
ENGINE_ID="your-enterprise-engine-id"

# The name of the Google Cloud Storage bucket for staging raw documents.
# This bucket is used by the ingestion pipeline.
GCS_BUCKET_NAME="your-gcs-bucket-name"

# --- Vertex AI Configuration ---

# The specific Google Cloud region for running Vertex AI models.
# Example: "us-central1"
VERTEX_AI_REGION="us-central1"

# --- Gemini API Configuration ---

# Your API key for the Gemini API, required for the agent to function.
# Note: This is not a Google Cloud API key.
# Generate one from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key"

